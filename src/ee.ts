import { html2json, json2html, Node } from "html2json";

export class NodeResolver {
  
  // static getNodeChildren(node: Node, children: Node): Node {

  //   if (node.tag === "children") {
  //     node.child = children;
  //     return node;
  //   } else {
  //     return NodeResolver.getNodeChildren(node, children);
  //   }
  // }
}